Put your looping background video here as:

  hero-bg.mp4

(Optional) add a poster frame image too:

  hero-poster.jpg   -- shown while the video loads, and on very slow connections

Recommended: 1920x1080 or 1280x720, under ~8MB, no audio needed (it's muted anyway),
5-15 seconds long since it loops. Something like torch-lit temple columns, sand
drifting over dunes, or a slow pan across hieroglyphs works well with the gold/black
theme.

Until you add hero-bg.mp4, the site won't break — index.html and home.html
automatically fall back to the existing gradient + pattern background.
